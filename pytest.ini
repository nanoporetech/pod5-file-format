[pytest]
minversion = 6.0

addopts =
    # Report details on all non-passing tests
    -ra
    --verbose
    --color=yes

# Warnings are errors
filterwarnings =
    error
    ignore::DeprecationWarning

# Find tests in both python projects
testpaths =
    python/lib_pod5/
    python/pod5/
